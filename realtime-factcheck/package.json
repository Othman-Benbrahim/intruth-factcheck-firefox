{
  "name": "intruth-factcheck-firefox",
  "version": "2.1.0",
  "private": true,
  "description": "Filet de tests de l'extension Firefox InTruth. Zero dependance : utilise le lanceur integre a Node (node:test).",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:watch": "node --test --watch \"tests/**/*.test.mjs\"",
    "test:spec": "node --test --test-reporter=spec \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
